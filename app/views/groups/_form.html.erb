<div class="row">
  <div class="span6 offset3">
    <%= form_for(@group) do |f| %>
    <%= render 'shared/error_messages', object: f.object %>
    <%= render 'fields', f: f %>
    <%= f.submit "Create a group", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>